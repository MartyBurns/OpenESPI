/*******************************************************************************
 * Copyright (c) 2011, 2012 EnergyOS.Org
 *
 * Licensed by EnergyOS.Org under one or more contributor license agreements.
 * See the NOTICE file distributed with this work for additional information
 * regarding copyright ownership.  The EnergyOS.org licenses this file
 * to you under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License.  You may obtain a copy
 * of the License at:
 *  
 *   http://www.apache.org/licenses/LICENSE-2.0
 *  
 *  Unless required by applicable law or agreed to in writing,
 *  software distributed under the License is distributed on an
 *  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 *  KIND, either express or implied.  See the License for the
 *  specific language governing permissions and limitations
 *  under the License.
 *  
 ******************************************************************************
*/


// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.datacustodian.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.energyos.espi.datacustodian.common.DataCustodianApplicationStatusDataOnDemand;
import org.energyos.espi.datacustodian.common.DataCustodianTypeDataOnDemand;
import org.energyos.espi.datacustodian.common.ServiceStatusDataOnDemand;
import org.energyos.espi.datacustodian.domain.DataCustodian;
import org.energyos.espi.datacustodian.domain.DataCustodianDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect DataCustodianDataOnDemand_Roo_DataOnDemand {
    
    declare @type: DataCustodianDataOnDemand: @Component;
    
    private Random DataCustodianDataOnDemand.rnd = new SecureRandom();
    
    private List<DataCustodian> DataCustodianDataOnDemand.data;
    
    @Autowired
    DataCustodianApplicationStatusDataOnDemand DataCustodianDataOnDemand.dataCustodianApplicationStatusDataOnDemand;
    
    @Autowired
    ServiceStatusDataOnDemand DataCustodianDataOnDemand.serviceStatusDataOnDemand;
    
    @Autowired
    DataCustodianTypeDataOnDemand DataCustodianDataOnDemand.dataCustodianTypeDataOnDemand;
    
    public DataCustodian DataCustodianDataOnDemand.getNewTransientDataCustodian(int index) {
        DataCustodian obj = new DataCustodian();
        setDescription(obj, index);
        setMRID(obj, index);
        setName(obj, index);
        return obj;
    }
    
    public void DataCustodianDataOnDemand.setDescription(DataCustodian obj, int index) {
        String description = "description_" + index;
        if (description.length() > 512) {
            description = description.substring(0, 512);
        }
        obj.setDescription(description);
    }
    
    public void DataCustodianDataOnDemand.setMRID(DataCustodian obj, int index) {
        String mRID = "mR_" + index;
        if (mRID.length() > 4) {
            mRID = mRID.substring(0, 4);
        }
        obj.setMRID(mRID);
    }
    
    public void DataCustodianDataOnDemand.setName(DataCustodian obj, int index) {
        String name = "name_" + index;
        if (name.length() > 30) {
            name = name.substring(0, 30);
        }
        obj.setName(name);
    }
    
    public DataCustodian DataCustodianDataOnDemand.getSpecificDataCustodian(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        DataCustodian obj = data.get(index);
        Long id = obj.getId();
        return DataCustodian.findDataCustodian(id);
    }
    
    public DataCustodian DataCustodianDataOnDemand.getRandomDataCustodian() {
        init();
        DataCustodian obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return DataCustodian.findDataCustodian(id);
    }
    
    public boolean DataCustodianDataOnDemand.modifyDataCustodian(DataCustodian obj) {
        return false;
    }
    
    public void DataCustodianDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = DataCustodian.findDataCustodianEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'DataCustodian' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<DataCustodian>();
        for (int i = 0; i < 10; i++) {
            DataCustodian obj = getNewTransientDataCustodian(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
