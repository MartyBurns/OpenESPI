// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.web;

import java.io.UnsupportedEncodingException;
import java.lang.Integer;
import java.lang.Long;
import java.lang.String;
import java.util.Collection;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.energyos.espi.thirdparty.domain.RetailCustomer;
import org.energyos.espi.thirdparty.domain.UsagePoint;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect UsagePointController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST)
    public String UsagePointController.create(@Valid UsagePoint usagePoint, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("usagePoint", usagePoint);
            return "usagepoints/create";
        }
        uiModel.asMap().clear();
        usagePoint.persist();
        return "redirect:/usagepoints/" + encodeUrlPathSegment(usagePoint.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", method = RequestMethod.GET)
    public String UsagePointController.createForm(Model uiModel) {
        uiModel.addAttribute("usagePoint", new UsagePoint());
        return "usagepoints/create";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.GET)
    public String UsagePointController.show(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("usagepoint", UsagePoint.findUsagePoint(id));
        uiModel.addAttribute("itemId", id);
        return "usagepoints/show";
    }
    
    @RequestMapping(method = RequestMethod.GET)
    public String UsagePointController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            uiModel.addAttribute("usagepoints", UsagePoint.findUsagePointEntries(page == null ? 0 : (page.intValue() - 1) * sizeNo, sizeNo));
            float nrOfPages = (float) UsagePoint.countUsagePoints() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("usagepoints", UsagePoint.findAllUsagePoints());
        }
        return "usagepoints/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT)
    public String UsagePointController.update(@Valid UsagePoint usagePoint, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("usagePoint", usagePoint);
            return "usagepoints/update";
        }
        uiModel.asMap().clear();
        usagePoint.merge();
        return "redirect:/usagepoints/" + encodeUrlPathSegment(usagePoint.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", method = RequestMethod.GET)
    public String UsagePointController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("usagePoint", UsagePoint.findUsagePoint(id));
        return "usagepoints/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE)
    public String UsagePointController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        UsagePoint.findUsagePoint(id).remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/usagepoints";
    }
    
    @ModelAttribute("retailcustomers")
    public Collection<RetailCustomer> UsagePointController.populateRetailCustomers() {
        return RetailCustomer.findAllRetailCustomers();
    }
    
    @ModelAttribute("usagepoints")
    public Collection<UsagePoint> UsagePointController.populateUsagePoints() {
        return UsagePoint.findAllUsagePoints();
    }
    
    String UsagePointController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        }
        catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
