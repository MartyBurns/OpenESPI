// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.web;

import java.io.UnsupportedEncodingException;
import java.lang.Integer;
import java.lang.Long;
import java.lang.String;
import java.util.Collection;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.energyos.espi.thirdparty.domain.ThirdParty;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect ThirdPartyController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST)
    public String ThirdPartyController.create(@Valid ThirdParty thirdParty, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("thirdParty", thirdParty);
            return "thirdpartys/create";
        }
        uiModel.asMap().clear();
        thirdParty.persist();
        return "redirect:/thirdpartys/" + encodeUrlPathSegment(thirdParty.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", method = RequestMethod.GET)
    public String ThirdPartyController.createForm(Model uiModel) {
        uiModel.addAttribute("thirdParty", new ThirdParty());
        return "thirdpartys/create";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.GET)
    public String ThirdPartyController.show(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("thirdparty", ThirdParty.findThirdParty(id));
        uiModel.addAttribute("itemId", id);
        return "thirdpartys/show";
    }
    
    @RequestMapping(method = RequestMethod.GET)
    public String ThirdPartyController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            uiModel.addAttribute("thirdpartys", ThirdParty.findThirdPartyEntries(page == null ? 0 : (page.intValue() - 1) * sizeNo, sizeNo));
            float nrOfPages = (float) ThirdParty.countThirdPartys() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("thirdpartys", ThirdParty.findAllThirdPartys());
        }
        return "thirdpartys/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT)
    public String ThirdPartyController.update(@Valid ThirdParty thirdParty, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("thirdParty", thirdParty);
            return "thirdpartys/update";
        }
        uiModel.asMap().clear();
        thirdParty.merge();
        return "redirect:/thirdpartys/" + encodeUrlPathSegment(thirdParty.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", method = RequestMethod.GET)
    public String ThirdPartyController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("thirdParty", ThirdParty.findThirdParty(id));
        return "thirdpartys/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE)
    public String ThirdPartyController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        ThirdParty.findThirdParty(id).remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/thirdpartys";
    }
    
    @ModelAttribute("thirdpartys")
    public Collection<ThirdParty> ThirdPartyController.populateThirdPartys() {
        return ThirdParty.findAllThirdPartys();
    }
    
    String ThirdPartyController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        }
        catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
