/*******************************************************************************
 * Copyright (c) 2011, 2012 EnergyOS.Org
 *
 * Licensed by EnergyOS.Org under one or more contributor license agreements.
 * See the NOTICE file distributed with this work for additional information
 * regarding copyright ownership.  The EnergyOS.org licenses this file
 * to you under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License.  You may obtain a copy
 * of the License at:
 *  
 *   http://www.apache.org/licenses/LICENSE-2.0
 *  
 *  Unless required by applicable law or agreed to in writing,
 *  software distributed under the License is distributed on an
 *  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 *  KIND, either express or implied.  See the License for the
 *  specific language governing permissions and limitations
 *  under the License.
 *  
 ******************************************************************************
*/

// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.web;

import java.io.UnsupportedEncodingException;
import java.util.ArrayList;
import java.util.List;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.energyos.espi.thirdparty.common.DateTimeInterval;
import org.energyos.espi.thirdparty.common.ElectricPowerQualitySummary;
import org.energyos.espi.thirdparty.web.ElectricPowerQualitySummaryController;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect ElectricPowerQualitySummaryController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String ElectricPowerQualitySummaryController.create(@Valid ElectricPowerQualitySummary electricPowerQualitySummary, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, electricPowerQualitySummary);
            return "electricpowerqualitysummarys/create";
        }
        uiModel.asMap().clear();
        electricPowerQualitySummary.persist();
        return "redirect:/electricpowerqualitysummarys/" + encodeUrlPathSegment(electricPowerQualitySummary.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String ElectricPowerQualitySummaryController.createForm(Model uiModel) {
        populateEditForm(uiModel, new ElectricPowerQualitySummary());
        List<String[]> dependencies = new ArrayList<String[]>();
        if (DateTimeInterval.countDateTimeIntervals() == 0) {
            dependencies.add(new String[] { "datetimeinterval", "datetimeintervals" });
        }
        uiModel.addAttribute("dependencies", dependencies);
        return "electricpowerqualitysummarys/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String ElectricPowerQualitySummaryController.show(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("electricpowerqualitysummary", ElectricPowerQualitySummary.findElectricPowerQualitySummary(id));
        uiModel.addAttribute("itemId", id);
        return "electricpowerqualitysummarys/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String ElectricPowerQualitySummaryController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("electricpowerqualitysummarys", ElectricPowerQualitySummary.findElectricPowerQualitySummaryEntries(firstResult, sizeNo));
            float nrOfPages = (float) ElectricPowerQualitySummary.countElectricPowerQualitySummarys() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("electricpowerqualitysummarys", ElectricPowerQualitySummary.findAllElectricPowerQualitySummarys());
        }
        return "electricpowerqualitysummarys/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String ElectricPowerQualitySummaryController.update(@Valid ElectricPowerQualitySummary electricPowerQualitySummary, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, electricPowerQualitySummary);
            return "electricpowerqualitysummarys/update";
        }
        uiModel.asMap().clear();
        electricPowerQualitySummary.merge();
        return "redirect:/electricpowerqualitysummarys/" + encodeUrlPathSegment(electricPowerQualitySummary.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String ElectricPowerQualitySummaryController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        populateEditForm(uiModel, ElectricPowerQualitySummary.findElectricPowerQualitySummary(id));
        return "electricpowerqualitysummarys/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String ElectricPowerQualitySummaryController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        ElectricPowerQualitySummary electricPowerQualitySummary = ElectricPowerQualitySummary.findElectricPowerQualitySummary(id);
        electricPowerQualitySummary.remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/electricpowerqualitysummarys";
    }
    
    void ElectricPowerQualitySummaryController.populateEditForm(Model uiModel, ElectricPowerQualitySummary electricPowerQualitySummary) {
        uiModel.addAttribute("electricPowerQualitySummary", electricPowerQualitySummary);
        uiModel.addAttribute("datetimeintervals", DateTimeInterval.findAllDateTimeIntervals());
    }
    
    String ElectricPowerQualitySummaryController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
