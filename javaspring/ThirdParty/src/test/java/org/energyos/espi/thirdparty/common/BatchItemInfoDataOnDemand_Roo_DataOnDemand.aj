/*******************************************************************************
 * Copyright (c) 2011, 2012 EnergyOS.Org
 *
 * Licensed by EnergyOS.Org under one or more contributor license agreements.
 * See the NOTICE file distributed with this work for additional information
 * regarding copyright ownership.  The EnergyOS.org licenses this file
 * to you under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License.  You may obtain a copy
 * of the License at:
 *  
 *   http://www.apache.org/licenses/LICENSE-2.0
 *  
 *  Unless required by applicable law or agreed to in writing,
 *  software distributed under the License is distributed on an
 *  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 *  KIND, either express or implied.  See the License for the
 *  specific language governing permissions and limitations
 *  under the License.
 *  
 ******************************************************************************
*/

// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.common;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.energyos.espi.thirdparty.common.BatchItemInfo;
import org.energyos.espi.thirdparty.common.BatchItemInfoDataOnDemand;
import org.energyos.espi.thirdparty.common.CRUDOperationDataOnDemand;
import org.energyos.espi.thirdparty.common.StatusCodeDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect BatchItemInfoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BatchItemInfoDataOnDemand: @Component;
    
    private Random BatchItemInfoDataOnDemand.rnd = new SecureRandom();
    
    private List<BatchItemInfo> BatchItemInfoDataOnDemand.data;
    
    @Autowired
    private CRUDOperationDataOnDemand BatchItemInfoDataOnDemand.cRUDOperationDataOnDemand;
    
    @Autowired
    private StatusCodeDataOnDemand BatchItemInfoDataOnDemand.statusCodeDataOnDemand;
    
    public BatchItemInfo BatchItemInfoDataOnDemand.getNewTransientBatchItemInfo(int index) {
        BatchItemInfo obj = new BatchItemInfo();
        setName(obj, index);
        setStatusReason(obj, index);
        return obj;
    }
    
    public void BatchItemInfoDataOnDemand.setName(BatchItemInfo obj, int index) {
        String name = "namexxxxxxxxxx_" + index;
        if (name.length() > 16) {
            name = name.substring(0, 16);
        }
        obj.setName(name);
    }
    
    public void BatchItemInfoDataOnDemand.setStatusReason(BatchItemInfo obj, int index) {
        String statusReason = "statusReason_" + index;
        if (statusReason.length() > 256) {
            statusReason = statusReason.substring(0, 256);
        }
        obj.setStatusReason(statusReason);
    }
    
    public BatchItemInfo BatchItemInfoDataOnDemand.getSpecificBatchItemInfo(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        BatchItemInfo obj = data.get(index);
        Long id = obj.getId();
        return BatchItemInfo.findBatchItemInfo(id);
    }
    
    public BatchItemInfo BatchItemInfoDataOnDemand.getRandomBatchItemInfo() {
        init();
        BatchItemInfo obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return BatchItemInfo.findBatchItemInfo(id);
    }
    
    public boolean BatchItemInfoDataOnDemand.modifyBatchItemInfo(BatchItemInfo obj) {
        return false;
    }
    
    public void BatchItemInfoDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = BatchItemInfo.findBatchItemInfoEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'BatchItemInfo' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<BatchItemInfo>();
        for (int i = 0; i < 10; i++) {
            BatchItemInfo obj = getNewTransientBatchItemInfo(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
