/*******************************************************************************
 * Copyright (c) 2011, 2012 EnergyOS.Org
 *
 * Licensed by EnergyOS.Org under one or more contributor license agreements.
 * See the NOTICE file distributed with this work for additional information
 * regarding copyright ownership.  The EnergyOS.org licenses this file
 * to you under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License.  You may obtain a copy
 * of the License at:
 *  
 *   http://www.apache.org/licenses/LICENSE-2.0
 *  
 *  Unless required by applicable law or agreed to in writing,
 *  software distributed under the License is distributed on an
 *  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 *  KIND, either express or implied.  See the License for the
 *  specific language governing permissions and limitations
 *  under the License.
 *  
 ******************************************************************************
*/

// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.common;

import java.util.List;
import org.energyos.espi.thirdparty.common.PhaseCode;
import org.energyos.espi.thirdparty.common.PhaseCodeDataOnDemand;
import org.energyos.espi.thirdparty.common.PhaseCodeIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PhaseCodeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PhaseCodeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PhaseCodeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PhaseCodeIntegrationTest: @Transactional;
    
    @Autowired
    private PhaseCodeDataOnDemand PhaseCodeIntegrationTest.dod;
    
    @Test
    public void PhaseCodeIntegrationTest.testCountPhaseCodes() {
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to initialize correctly", dod.getRandomPhaseCode());
        long count = PhaseCode.countPhaseCodes();
        Assert.assertTrue("Counter for 'PhaseCode' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PhaseCodeIntegrationTest.testFindPhaseCode() {
        PhaseCode obj = dod.getRandomPhaseCode();
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to provide an identifier", id);
        obj = PhaseCode.findPhaseCode(id);
        Assert.assertNotNull("Find method for 'PhaseCode' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PhaseCode' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PhaseCodeIntegrationTest.testFindAllPhaseCodes() {
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to initialize correctly", dod.getRandomPhaseCode());
        long count = PhaseCode.countPhaseCodes();
        Assert.assertTrue("Too expensive to perform a find all test for 'PhaseCode', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PhaseCode> result = PhaseCode.findAllPhaseCodes();
        Assert.assertNotNull("Find all method for 'PhaseCode' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PhaseCode' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PhaseCodeIntegrationTest.testFindPhaseCodeEntries() {
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to initialize correctly", dod.getRandomPhaseCode());
        long count = PhaseCode.countPhaseCodes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PhaseCode> result = PhaseCode.findPhaseCodeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PhaseCode' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PhaseCode' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PhaseCodeIntegrationTest.testFlush() {
        PhaseCode obj = dod.getRandomPhaseCode();
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to provide an identifier", id);
        obj = PhaseCode.findPhaseCode(id);
        Assert.assertNotNull("Find method for 'PhaseCode' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyPhaseCode(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'PhaseCode' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PhaseCodeIntegrationTest.testMergeUpdate() {
        PhaseCode obj = dod.getRandomPhaseCode();
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to provide an identifier", id);
        obj = PhaseCode.findPhaseCode(id);
        boolean modified =  dod.modifyPhaseCode(obj);
        Integer currentVersion = obj.getVersion();
        PhaseCode merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'PhaseCode' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PhaseCodeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to initialize correctly", dod.getRandomPhaseCode());
        PhaseCode obj = dod.getNewTransientPhaseCode(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PhaseCode' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'PhaseCode' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PhaseCodeIntegrationTest.testRemove() {
        PhaseCode obj = dod.getRandomPhaseCode();
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PhaseCode' failed to provide an identifier", id);
        obj = PhaseCode.findPhaseCode(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'PhaseCode' with identifier '" + id + "'", PhaseCode.findPhaseCode(id));
    }
    
}
