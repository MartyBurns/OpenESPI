/*******************************************************************************
 * Copyright (c) 2011, 2012 EnergyOS.Org
 *
 * Licensed by EnergyOS.Org under one or more contributor license agreements.
 * See the NOTICE file distributed with this work for additional information
 * regarding copyright ownership.  The EnergyOS.org licenses this file
 * to you under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License.  You may obtain a copy
 * of the License at:
 *  
 *   http://www.apache.org/licenses/LICENSE-2.0
 *  
 *  Unless required by applicable law or agreed to in writing,
 *  software distributed under the License is distributed on an
 *  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 *  KIND, either express or implied.  See the License for the
 *  specific language governing permissions and limitations
 *  under the License.
 *  
 ******************************************************************************
*/

// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.common;

import java.util.List;
import org.energyos.espi.thirdparty.common.RationalNumber;
import org.energyos.espi.thirdparty.common.RationalNumberDataOnDemand;
import org.energyos.espi.thirdparty.common.RationalNumberIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect RationalNumberIntegrationTest_Roo_IntegrationTest {
    
    declare @type: RationalNumberIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: RationalNumberIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: RationalNumberIntegrationTest: @Transactional;
    
    @Autowired
    private RationalNumberDataOnDemand RationalNumberIntegrationTest.dod;
    
    @Test
    public void RationalNumberIntegrationTest.testCountRationalNumbers() {
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to initialize correctly", dod.getRandomRationalNumber());
        long count = RationalNumber.countRationalNumbers();
        Assert.assertTrue("Counter for 'RationalNumber' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void RationalNumberIntegrationTest.testFindRationalNumber() {
        RationalNumber obj = dod.getRandomRationalNumber();
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to provide an identifier", id);
        obj = RationalNumber.findRationalNumber(id);
        Assert.assertNotNull("Find method for 'RationalNumber' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'RationalNumber' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void RationalNumberIntegrationTest.testFindAllRationalNumbers() {
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to initialize correctly", dod.getRandomRationalNumber());
        long count = RationalNumber.countRationalNumbers();
        Assert.assertTrue("Too expensive to perform a find all test for 'RationalNumber', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<RationalNumber> result = RationalNumber.findAllRationalNumbers();
        Assert.assertNotNull("Find all method for 'RationalNumber' illegally returned null", result);
        Assert.assertTrue("Find all method for 'RationalNumber' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void RationalNumberIntegrationTest.testFindRationalNumberEntries() {
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to initialize correctly", dod.getRandomRationalNumber());
        long count = RationalNumber.countRationalNumbers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<RationalNumber> result = RationalNumber.findRationalNumberEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'RationalNumber' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'RationalNumber' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void RationalNumberIntegrationTest.testFlush() {
        RationalNumber obj = dod.getRandomRationalNumber();
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to provide an identifier", id);
        obj = RationalNumber.findRationalNumber(id);
        Assert.assertNotNull("Find method for 'RationalNumber' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyRationalNumber(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'RationalNumber' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void RationalNumberIntegrationTest.testMergeUpdate() {
        RationalNumber obj = dod.getRandomRationalNumber();
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to provide an identifier", id);
        obj = RationalNumber.findRationalNumber(id);
        boolean modified =  dod.modifyRationalNumber(obj);
        Integer currentVersion = obj.getVersion();
        RationalNumber merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'RationalNumber' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void RationalNumberIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to initialize correctly", dod.getRandomRationalNumber());
        RationalNumber obj = dod.getNewTransientRationalNumber(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'RationalNumber' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'RationalNumber' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void RationalNumberIntegrationTest.testRemove() {
        RationalNumber obj = dod.getRandomRationalNumber();
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RationalNumber' failed to provide an identifier", id);
        obj = RationalNumber.findRationalNumber(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'RationalNumber' with identifier '" + id + "'", RationalNumber.findRationalNumber(id));
    }
    
}
