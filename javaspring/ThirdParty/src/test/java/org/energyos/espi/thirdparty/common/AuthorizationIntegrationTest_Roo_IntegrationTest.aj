/*******************************************************************************
 * Copyright (c) 2011, 2012 EnergyOS.Org
 *
 * Licensed by EnergyOS.Org under one or more contributor license agreements.
 * See the NOTICE file distributed with this work for additional information
 * regarding copyright ownership.  The EnergyOS.org licenses this file
 * to you under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License.  You may obtain a copy
 * of the License at:
 *  
 *   http://www.apache.org/licenses/LICENSE-2.0
 *  
 *  Unless required by applicable law or agreed to in writing,
 *  software distributed under the License is distributed on an
 *  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 *  KIND, either express or implied.  See the License for the
 *  specific language governing permissions and limitations
 *  under the License.
 *  
 ******************************************************************************
*/


// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.common;

import java.util.List;
import org.energyos.espi.thirdparty.common.Authorization;
import org.energyos.espi.thirdparty.common.AuthorizationDataOnDemand;
import org.energyos.espi.thirdparty.common.AuthorizationIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AuthorizationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AuthorizationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AuthorizationIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AuthorizationIntegrationTest: @Transactional;
    
    @Autowired
    AuthorizationDataOnDemand AuthorizationIntegrationTest.dod;
    
    @Test
    public void AuthorizationIntegrationTest.testCountAuthorizations() {
        Assert.assertNotNull("Data on demand for 'Authorization' failed to initialize correctly", dod.getRandomAuthorization());
        long count = Authorization.countAuthorizations();
        Assert.assertTrue("Counter for 'Authorization' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AuthorizationIntegrationTest.testFindAuthorization() {
        Authorization obj = dod.getRandomAuthorization();
        Assert.assertNotNull("Data on demand for 'Authorization' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Authorization' failed to provide an identifier", id);
        obj = Authorization.findAuthorization(id);
        Assert.assertNotNull("Find method for 'Authorization' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Authorization' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AuthorizationIntegrationTest.testFindAllAuthorizations() {
        Assert.assertNotNull("Data on demand for 'Authorization' failed to initialize correctly", dod.getRandomAuthorization());
        long count = Authorization.countAuthorizations();
        Assert.assertTrue("Too expensive to perform a find all test for 'Authorization', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Authorization> result = Authorization.findAllAuthorizations();
        Assert.assertNotNull("Find all method for 'Authorization' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Authorization' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AuthorizationIntegrationTest.testFindAuthorizationEntries() {
        Assert.assertNotNull("Data on demand for 'Authorization' failed to initialize correctly", dod.getRandomAuthorization());
        long count = Authorization.countAuthorizations();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Authorization> result = Authorization.findAuthorizationEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Authorization' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Authorization' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AuthorizationIntegrationTest.testFlush() {
        Authorization obj = dod.getRandomAuthorization();
        Assert.assertNotNull("Data on demand for 'Authorization' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Authorization' failed to provide an identifier", id);
        obj = Authorization.findAuthorization(id);
        Assert.assertNotNull("Find method for 'Authorization' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyAuthorization(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'Authorization' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AuthorizationIntegrationTest.testMergeUpdate() {
        Authorization obj = dod.getRandomAuthorization();
        Assert.assertNotNull("Data on demand for 'Authorization' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Authorization' failed to provide an identifier", id);
        obj = Authorization.findAuthorization(id);
        boolean modified =  dod.modifyAuthorization(obj);
        Integer currentVersion = obj.getVersion();
        Authorization merged = (Authorization)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'Authorization' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void AuthorizationIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'Authorization' failed to initialize correctly", dod.getRandomAuthorization());
        Authorization obj = dod.getNewTransientAuthorization(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Authorization' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Authorization' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'Authorization' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AuthorizationIntegrationTest.testRemove() {
        Authorization obj = dod.getRandomAuthorization();
        Assert.assertNotNull("Data on demand for 'Authorization' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Authorization' failed to provide an identifier", id);
        obj = Authorization.findAuthorization(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'Authorization' with identifier '" + id + "'", Authorization.findAuthorization(id));
    }
    
}
