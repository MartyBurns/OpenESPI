/*******************************************************************************
 * Copyright (c) 2011, 2012 EnergyOS.Org
 *
 * Licensed by EnergyOS.Org under one or more contributor license agreements.
 * See the NOTICE file distributed with this work for additional information
 * regarding copyright ownership.  The EnergyOS.org licenses this file
 * to you under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License.  You may obtain a copy
 * of the License at:
 *  
 *   http://www.apache.org/licenses/LICENSE-2.0
 *  
 *  Unless required by applicable law or agreed to in writing,
 *  software distributed under the License is distributed on an
 *  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 *  KIND, either express or implied.  See the License for the
 *  specific language governing permissions and limitations
 *  under the License.
 *  
 ******************************************************************************
*/


// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.common;

import java.util.List;
import org.energyos.espi.thirdparty.common.ServiceKind;
import org.energyos.espi.thirdparty.common.ServiceKindDataOnDemand;
import org.energyos.espi.thirdparty.common.ServiceKindIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ServiceKindIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ServiceKindIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ServiceKindIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ServiceKindIntegrationTest: @Transactional;
    
    @Autowired
    ServiceKindDataOnDemand ServiceKindIntegrationTest.dod;
    
    @Test
    public void ServiceKindIntegrationTest.testCountServiceKinds() {
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to initialize correctly", dod.getRandomServiceKind());
        long count = ServiceKind.countServiceKinds();
        Assert.assertTrue("Counter for 'ServiceKind' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ServiceKindIntegrationTest.testFindServiceKind() {
        ServiceKind obj = dod.getRandomServiceKind();
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to provide an identifier", id);
        obj = ServiceKind.findServiceKind(id);
        Assert.assertNotNull("Find method for 'ServiceKind' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ServiceKind' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ServiceKindIntegrationTest.testFindAllServiceKinds() {
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to initialize correctly", dod.getRandomServiceKind());
        long count = ServiceKind.countServiceKinds();
        Assert.assertTrue("Too expensive to perform a find all test for 'ServiceKind', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ServiceKind> result = ServiceKind.findAllServiceKinds();
        Assert.assertNotNull("Find all method for 'ServiceKind' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ServiceKind' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ServiceKindIntegrationTest.testFindServiceKindEntries() {
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to initialize correctly", dod.getRandomServiceKind());
        long count = ServiceKind.countServiceKinds();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ServiceKind> result = ServiceKind.findServiceKindEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ServiceKind' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ServiceKind' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ServiceKindIntegrationTest.testFlush() {
        ServiceKind obj = dod.getRandomServiceKind();
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to provide an identifier", id);
        obj = ServiceKind.findServiceKind(id);
        Assert.assertNotNull("Find method for 'ServiceKind' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyServiceKind(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ServiceKind' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ServiceKindIntegrationTest.testMergeUpdate() {
        ServiceKind obj = dod.getRandomServiceKind();
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to provide an identifier", id);
        obj = ServiceKind.findServiceKind(id);
        boolean modified =  dod.modifyServiceKind(obj);
        Integer currentVersion = obj.getVersion();
        ServiceKind merged = obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ServiceKind' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ServiceKindIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to initialize correctly", dod.getRandomServiceKind());
        ServiceKind obj = dod.getNewTransientServiceKind(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ServiceKind' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ServiceKind' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ServiceKindIntegrationTest.testRemove() {
        ServiceKind obj = dod.getRandomServiceKind();
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ServiceKind' failed to provide an identifier", id);
        obj = ServiceKind.findServiceKind(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ServiceKind' with identifier '" + id + "'", ServiceKind.findServiceKind(id));
    }
    
}
