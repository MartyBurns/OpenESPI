/*******************************************************************************
 * Copyright (c) 2011, 2012 EnergyOS.Org
 *
 * Licensed by EnergyOS.Org under one or more contributor license agreements.
 * See the NOTICE file distributed with this work for additional information
 * regarding copyright ownership.  The EnergyOS.org licenses this file
 * to you under the Apache License, Version 2.0 (the "License"); you may not
 * use this file except in compliance with the License.  You may obtain a copy
 * of the License at:
 *  
 *   http://www.apache.org/licenses/LICENSE-2.0
 *  
 *  Unless required by applicable law or agreed to in writing,
 *  software distributed under the License is distributed on an
 *  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 *  KIND, either express or implied.  See the License for the
 *  specific language governing permissions and limitations
 *  under the License.
 *  
 ******************************************************************************
*/


// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.common;

import java.util.List;
import org.energyos.espi.thirdparty.common.ReadingType;
import org.energyos.espi.thirdparty.common.ReadingTypeDataOnDemand;
import org.energyos.espi.thirdparty.common.ReadingTypeIntegrationTest;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ReadingTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ReadingTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ReadingTypeIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ReadingTypeIntegrationTest: @Transactional;
    
    @Autowired
    ReadingTypeDataOnDemand ReadingTypeIntegrationTest.dod;
    
    @Test
    public void ReadingTypeIntegrationTest.testCountReadingTypes() {
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to initialize correctly", dod.getRandomReadingType());
        long count = ReadingType.countReadingTypes();
        Assert.assertTrue("Counter for 'ReadingType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ReadingTypeIntegrationTest.testFindReadingType() {
        ReadingType obj = dod.getRandomReadingType();
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to provide an identifier", id);
        obj = ReadingType.findReadingType(id);
        Assert.assertNotNull("Find method for 'ReadingType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ReadingType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ReadingTypeIntegrationTest.testFindAllReadingTypes() {
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to initialize correctly", dod.getRandomReadingType());
        long count = ReadingType.countReadingTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'ReadingType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ReadingType> result = ReadingType.findAllReadingTypes();
        Assert.assertNotNull("Find all method for 'ReadingType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ReadingType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ReadingTypeIntegrationTest.testFindReadingTypeEntries() {
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to initialize correctly", dod.getRandomReadingType());
        long count = ReadingType.countReadingTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ReadingType> result = ReadingType.findReadingTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ReadingType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ReadingType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ReadingTypeIntegrationTest.testFlush() {
        ReadingType obj = dod.getRandomReadingType();
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to provide an identifier", id);
        obj = ReadingType.findReadingType(id);
        Assert.assertNotNull("Find method for 'ReadingType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyReadingType(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ReadingType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ReadingTypeIntegrationTest.testMergeUpdate() {
        ReadingType obj = dod.getRandomReadingType();
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to provide an identifier", id);
        obj = ReadingType.findReadingType(id);
        boolean modified =  dod.modifyReadingType(obj);
        Integer currentVersion = obj.getVersion();
        ReadingType merged = (ReadingType)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ReadingType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ReadingTypeIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to initialize correctly", dod.getRandomReadingType());
        ReadingType obj = dod.getNewTransientReadingType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ReadingType' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        Assert.assertNotNull("Expected 'ReadingType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ReadingTypeIntegrationTest.testRemove() {
        ReadingType obj = dod.getRandomReadingType();
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ReadingType' failed to provide an identifier", id);
        obj = ReadingType.findReadingType(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ReadingType' with identifier '" + id + "'", ReadingType.findReadingType(id));
    }
    
}
