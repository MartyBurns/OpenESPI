// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.web;

import java.io.UnsupportedEncodingException;
import java.lang.Integer;
import java.lang.Long;
import java.lang.String;
import java.util.Collection;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.energyos.espi.thirdparty.domain.RetailCustomer;
import org.joda.time.format.DateTimeFormat;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect RetailCustomerController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST)
    public String RetailCustomerController.create(@Valid RetailCustomer retailCustomer, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("retailCustomer", retailCustomer);
            addDateTimeFormatPatterns(uiModel);
            return "retailcustomers/create";
        }
        uiModel.asMap().clear();
        retailCustomer.persist();
        return "redirect:/retailcustomers/" + encodeUrlPathSegment(retailCustomer.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", method = RequestMethod.GET)
    public String RetailCustomerController.createForm(Model uiModel) {
        uiModel.addAttribute("retailCustomer", new RetailCustomer());
        addDateTimeFormatPatterns(uiModel);
        return "retailcustomers/create";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.GET)
    public String RetailCustomerController.show(@PathVariable("id") Long id, Model uiModel) {
        addDateTimeFormatPatterns(uiModel);
        uiModel.addAttribute("retailcustomer", RetailCustomer.findRetailCustomer(id));
        uiModel.addAttribute("itemId", id);
        return "retailcustomers/show";
    }
    
    @RequestMapping(method = RequestMethod.GET)
    public String RetailCustomerController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            uiModel.addAttribute("retailcustomers", RetailCustomer.findRetailCustomerEntries(page == null ? 0 : (page.intValue() - 1) * sizeNo, sizeNo));
            float nrOfPages = (float) RetailCustomer.countRetailCustomers() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("retailcustomers", RetailCustomer.findAllRetailCustomers());
        }
        addDateTimeFormatPatterns(uiModel);
        return "retailcustomers/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT)
    public String RetailCustomerController.update(@Valid RetailCustomer retailCustomer, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            uiModel.addAttribute("retailCustomer", retailCustomer);
            addDateTimeFormatPatterns(uiModel);
            return "retailcustomers/update";
        }
        uiModel.asMap().clear();
        retailCustomer.merge();
        return "redirect:/retailcustomers/" + encodeUrlPathSegment(retailCustomer.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", method = RequestMethod.GET)
    public String RetailCustomerController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("retailCustomer", RetailCustomer.findRetailCustomer(id));
        addDateTimeFormatPatterns(uiModel);
        return "retailcustomers/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE)
    public String RetailCustomerController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        RetailCustomer.findRetailCustomer(id).remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/retailcustomers";
    }
    
    @ModelAttribute("retailcustomers")
    public Collection<RetailCustomer> RetailCustomerController.populateRetailCustomers() {
        return RetailCustomer.findAllRetailCustomers();
    }
    
    void RetailCustomerController.addDateTimeFormatPatterns(Model uiModel) {
        uiModel.addAttribute("retailCustomer_customersince_date_format", DateTimeFormat.patternForStyle("M-", LocaleContextHolder.getLocale()));
    }
    
    String RetailCustomerController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        }
        catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
