// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.energyos.espi.thirdparty.domain;

import java.lang.Integer;
import java.lang.Long;
import java.util.List;
import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.EntityManager;
import javax.persistence.GeneratedValue;
import javax.persistence.GenerationType;
import javax.persistence.Id;
import javax.persistence.PersistenceContext;
import javax.persistence.Version;
import org.energyos.espi.thirdparty.domain.ThirdParty;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ThirdParty_Roo_Entity {
    
    declare @type: ThirdParty: @Entity;
    
    @PersistenceContext
    transient EntityManager ThirdParty.entityManager;
    
    @Id
    @GeneratedValue(strategy = GenerationType.AUTO)
    @Column(name = "id")
    private Long ThirdParty.id;
    
    @Version
    @Column(name = "version")
    private Integer ThirdParty.version;
    
    public Long ThirdParty.getId() {
        return this.id;
    }
    
    public void ThirdParty.setId(Long id) {
        this.id = id;
    }
    
    public Integer ThirdParty.getVersion() {
        return this.version;
    }
    
    public void ThirdParty.setVersion(Integer version) {
        this.version = version;
    }
    
    @Transactional
    public void ThirdParty.persist() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.persist(this);
    }
    
    @Transactional
    public void ThirdParty.remove() {
        if (this.entityManager == null) this.entityManager = entityManager();
        if (this.entityManager.contains(this)) {
            this.entityManager.remove(this);
        } else {
            ThirdParty attached = ThirdParty.findThirdParty(this.id);
            this.entityManager.remove(attached);
        }
    }
    
    @Transactional
    public void ThirdParty.flush() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.flush();
    }
    
    @Transactional
    public void ThirdParty.clear() {
        if (this.entityManager == null) this.entityManager = entityManager();
        this.entityManager.clear();
    }
    
    @Transactional
    public ThirdParty ThirdParty.merge() {
        if (this.entityManager == null) this.entityManager = entityManager();
        ThirdParty merged = this.entityManager.merge(this);
        this.entityManager.flush();
        return merged;
    }
    
    public static final EntityManager ThirdParty.entityManager() {
        EntityManager em = new ThirdParty().entityManager;
        if (em == null) throw new IllegalStateException("Entity manager has not been injected (is the Spring Aspects JAR configured as an AJC/AJDT aspects library?)");
        return em;
    }
    
    public static long ThirdParty.countThirdPartys() {
        return entityManager().createQuery("SELECT COUNT(o) FROM ThirdParty o", Long.class).getSingleResult();
    }
    
    public static List<ThirdParty> ThirdParty.findAllThirdPartys() {
        return entityManager().createQuery("SELECT o FROM ThirdParty o", ThirdParty.class).getResultList();
    }
    
    public static ThirdParty ThirdParty.findThirdParty(Long id) {
        if (id == null) return null;
        return entityManager().find(ThirdParty.class, id);
    }
    
    public static List<ThirdParty> ThirdParty.findThirdPartyEntries(int firstResult, int maxResults) {
        return entityManager().createQuery("SELECT o FROM ThirdParty o", ThirdParty.class).setFirstResult(firstResult).setMaxResults(maxResults).getResultList();
    }
    
}
